import { Box, Typography } from '@mui/material';
import React from 'react';

const Statistic = () => (
  <Box sx={{
    border: '1px solid', width: 500, height: 400, padding: '10px',
  }}
  >
    <Typography>Statistics</Typography>
    
  </Box>
);

export default Statistic;
